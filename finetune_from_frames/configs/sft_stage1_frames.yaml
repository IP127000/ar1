defaults:
  - sft_base_frames
  - /models/ar1_base@model
  - _self_

trainer:
  lr_multiplier:
    vlm.model.visual: 0.1
  ddp_find_unused_parameters: false
  gradient_accumulation_steps: 4
  learning_rate: 1e-5
  warmup_steps: 500

paths:
  output_dir: output_sft_frames_stage1

evaluate:
  eval_ckpt: null
